import { isNull, isObject, loadProperty } from "@tsparticles/engine";
export class SoundsVolume {
    max = 100;
    min = 0;
    step = 10;
    value = 100;
    load(data) {
        if (isNull(data)) {
            return;
        }
        if (isObject(data)) {
            loadProperty(this, "max", data.max);
            loadProperty(this, "min", data.min);
            loadProperty(this, "step", data.step);
            if (data.value !== undefined) {
                this.value = data.value;
            }
        }
        else {
            this.value = data;
        }
    }
}
