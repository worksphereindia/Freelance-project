import { ValueWithRandom, isNull, isNumber, loadProperty, } from "@tsparticles/engine";
import { AbsorberSizeLimit } from "./AbsorberSizeLimit.js";
export class AbsorberSize extends ValueWithRandom {
    density = 5;
    limit = new AbsorberSizeLimit();
    value = 50;
    load(data) {
        if (isNull(data)) {
            return;
        }
        super.load(data);
        loadProperty(this, "density", data.density);
        if (isNumber(data.limit)) {
            this.limit.radius = data.limit;
        }
        else {
            this.limit.load(data.limit);
        }
    }
}
