import { type IHsl, type Particle } from "@tsparticles/engine";
import type { OrbitContainer } from "./Types.js";
export declare function drawEllipse(container: OrbitContainer, context: OffscreenCanvasRenderingContext2D, particle: Particle, fillColorValue: IHsl | undefined, radius: number, opacity: number, width: number, rotation: number, start: number, end: number): void;
