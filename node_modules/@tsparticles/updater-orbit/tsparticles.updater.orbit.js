(function(g){g.__tsParticlesInternals=g.__tsParticlesInternals||{};g.__tsParticlesInternals.bundles=g.__tsParticlesInternals.bundles||{};g.__tsParticlesInternals.effects=g.__tsParticlesInternals.effects||{};g.__tsParticlesInternals.engine=g.__tsParticlesInternals.engine||{};g.__tsParticlesInternals.interactions=g.__tsParticlesInternals.interactions||{};g.__tsParticlesInternals.palettes=g.__tsParticlesInternals.palettes||{};g.__tsParticlesInternals.paths=g.__tsParticlesInternals.paths||{};g.__tsParticlesInternals.plugins=g.__tsParticlesInternals.plugins||{};g.__tsParticlesInternals.plugins=g.__tsParticlesInternals.plugins||{};g.__tsParticlesInternals.plugins.emittersShapes=g.__tsParticlesInternals.plugins.emittersShapes||{};g.__tsParticlesInternals.presets=g.__tsParticlesInternals.presets||{};g.__tsParticlesInternals.shapes=g.__tsParticlesInternals.shapes||{};g.__tsParticlesInternals.updaters=g.__tsParticlesInternals.updaters||{};g.__tsParticlesInternals.utils=g.__tsParticlesInternals.utils||{};g.__tsParticlesInternals.canvas=g.__tsParticlesInternals.canvas||{};g.__tsParticlesInternals.canvas=g.__tsParticlesInternals.canvas||{};g.__tsParticlesInternals.canvas.utils=g.__tsParticlesInternals.canvas.utils||{};g.__tsParticlesInternals.path=g.__tsParticlesInternals.path||{};g.__tsParticlesInternals.path=g.__tsParticlesInternals.path||{};g.__tsParticlesInternals.path.utils=g.__tsParticlesInternals.path.utils||{};var __tsProxyFactory=typeof Proxy!=="undefined"?function(obj){return new Proxy(obj,{get:function(target,key){if(!(key in target)){target[key]={};}return target[key];}});}:function(obj){return obj;};g.__tsParticlesInternals.bundles=__tsProxyFactory(g.__tsParticlesInternals.bundles);g.__tsParticlesInternals.effects=__tsProxyFactory(g.__tsParticlesInternals.effects);g.__tsParticlesInternals.interactions=__tsProxyFactory(g.__tsParticlesInternals.interactions);g.__tsParticlesInternals.palettes=__tsProxyFactory(g.__tsParticlesInternals.palettes);g.__tsParticlesInternals.paths=__tsProxyFactory(g.__tsParticlesInternals.paths);g.__tsParticlesInternals.plugins=__tsProxyFactory(g.__tsParticlesInternals.plugins);g.__tsParticlesInternals.plugins.emittersShapes=__tsProxyFactory(g.__tsParticlesInternals.plugins.emittersShapes);g.__tsParticlesInternals.presets=__tsProxyFactory(g.__tsParticlesInternals.presets);g.__tsParticlesInternals.shapes=__tsProxyFactory(g.__tsParticlesInternals.shapes);g.__tsParticlesInternals.updaters=__tsProxyFactory(g.__tsParticlesInternals.updaters);g.__tsParticlesInternals.utils=__tsProxyFactory(g.__tsParticlesInternals.utils);g.__tsParticlesInternals.canvas=__tsProxyFactory(g.__tsParticlesInternals.canvas);g.__tsParticlesInternals.path=__tsProxyFactory(g.__tsParticlesInternals.path);g.tsparticlesInternalExports=g.tsparticlesInternalExports||{};})(typeof globalThis!=="undefined"?globalThis:typeof window!=="undefined"?window:this);
/* Updater v4.3.2 */
(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('@tsparticles/engine')) :
    typeof define === 'function' && define.amd ? define(['exports', '@tsparticles/engine'], factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory((global.__tsParticlesInternals = global.__tsParticlesInternals || {}, global.__tsParticlesInternals.updaters = global.__tsParticlesInternals.updaters || {}, global.__tsParticlesInternals.updaters.orbit = global.__tsParticlesInternals.updaters.orbit || {}), global.__tsParticlesInternals.engine));
})(this, (function (exports, engine) { 'use strict';

    class OrbitRotation extends engine.ValueWithRandom {
        value = 45;
        load(data) {
            if (engine.isNull(data)) {
                return;
            }
            super.load(data);
        }
    }

    class Orbit {
        animation = new engine.AnimationOptions();
        color;
        enable = false;
        opacity = 1;
        radius;
        rotation = new OrbitRotation();
        width = 1;
        load(data) {
            if (engine.isNull(data)) {
                return;
            }
            this.animation.load(data.animation);
            this.rotation.load(data.rotation);
            engine.loadProperty(this, "enable", data.enable);
            engine.loadRangeProperty(this, "opacity", data.opacity);
            engine.loadRangeProperty(this, "width", data.width);
            engine.loadRangeProperty(this, "radius", data.radius);
            if (data.color !== undefined) {
                this.color = engine.OptionsColor.create(this.color, data.color);
            }
        }
    }

    var OrbitType;
    (function (OrbitType) {
        OrbitType["front"] = "front";
        OrbitType["back"] = "back";
    })(OrbitType || (OrbitType = {}));

    const minWidth = 0;
    function drawEllipse(container, context, particle, fillColorValue, radius, opacity, width, rotation, start, end) {
        if (width <= minWidth) {
            return;
        }
        const pos = particle.getPosition();
        if (fillColorValue) {
            context.strokeStyle = engine.getStyleFromHsl(fillColorValue, container.hdr, opacity);
        }
        context.lineWidth = width;
        const rotationRadian = engine.degToRad(rotation);
        context.beginPath();
        context.ellipse(pos.x, pos.y, radius * engine.half, radius * engine.double, rotationRadian, start, end);
        context.stroke();
    }

    const defaultOrbitSpeed = 0, halfPI = Math.PI * engine.half, piAndAHalf = Math.PI + halfPI, startAngle = 0, defaultWidth = 1, defaultRotation = 0;
    class OrbitUpdater {
        #container;
        #pluginManager;
        constructor(pluginManager, container) {
            this.#pluginManager = pluginManager;
            this.#container = container;
        }
        afterDraw(particle) {
            const orbitOptions = particle.options.orbit;
            if (orbitOptions?.enable) {
                this.drawOrbit(particle, OrbitType.front);
            }
        }
        beforeDraw(particle) {
            const orbitOptions = particle.options.orbit;
            if (orbitOptions?.enable) {
                this.drawOrbit(particle, OrbitType.back);
            }
        }
        drawOrbit(particle, type) {
            const container = this.#container;
            let start, end;
            switch (type) {
                case OrbitType.back:
                    start = halfPI;
                    end = piAndAHalf;
                    break;
                case OrbitType.front:
                    start = piAndAHalf;
                    end = halfPI;
                    break;
                default:
                    start = startAngle;
                    end = engine.doublePI;
            }
            container.canvas.render.draw(ctx => {
                drawEllipse(container, ctx, particle, particle.orbitColor ?? particle.getFillColor(), particle.retina.orbitRadius ?? container.retina.orbitRadius ?? particle.getRadius(), particle.orbitOpacity ?? engine.defaultOpacity, particle.orbitWidth ?? defaultWidth, (particle.orbitRotation ?? defaultRotation) * container.retina.pixelRatio, start, end);
            });
        }
        init(particle) {
            const container = this.#container, particlesOptions = particle.options, orbitOptions = particlesOptions.orbit;
            if (!orbitOptions?.enable) {
                return;
            }
            particle.orbitRotation = engine.getRangeValue(orbitOptions.rotation.value);
            particle.orbitColor = engine.rangeColorToHsl(this.#pluginManager, orbitOptions.color);
            particle.retina.orbitRadius =
                orbitOptions.radius !== undefined ? engine.getRangeValue(orbitOptions.radius) * container.retina.pixelRatio : undefined;
            container.retina.orbitRadius = particle.retina.orbitRadius;
            particle.orbitAnimationSpeed = orbitOptions.animation.enable
                ? engine.getRangeValue(orbitOptions.animation.speed)
                : defaultOrbitSpeed;
            particle.orbitWidth = engine.getRangeValue(orbitOptions.width);
            particle.orbitOpacity = engine.getRangeValue(orbitOptions.opacity);
        }
        isEnabled(particle) {
            const orbitAnimations = particle.options.orbit?.animation;
            return !particle.destroyed && !particle.spawning && !!orbitAnimations?.enable;
        }
        loadOptions(options, ...sources) {
            engine.loadOptionProperty(options, "orbit", Orbit, ...sources);
        }
        update(particle, delta) {
            if (!this.isEnabled(particle)) {
                return;
            }
            particle.orbitRotation ??= defaultRotation;
            particle.orbitRotation += (particle.orbitAnimationSpeed ?? defaultOrbitSpeed / engine.doublePI) * delta.factor;
        }
    }

    async function loadOrbitUpdater(engine) {
        engine.checkVersion("4.3.2");
        await engine.pluginManager.register(e => {
            e.pluginManager.addParticleUpdater("orbit", container => {
                return Promise.resolve(new OrbitUpdater(e.pluginManager, container));
            });
        });
    }

    const globalObject = globalThis;
    globalObject.__tsParticlesInternals = globalObject.__tsParticlesInternals ?? {};
    globalObject.loadOrbitUpdater = loadOrbitUpdater;

    exports.loadOrbitUpdater = loadOrbitUpdater;

}));
Object.assign(globalThis.window || globalThis, { loadOrbitUpdater: (globalThis.__tsParticlesInternals.updaters.orbit || {}).loadOrbitUpdater });
delete (globalThis.window || globalThis).tsparticlesInternalExports;
