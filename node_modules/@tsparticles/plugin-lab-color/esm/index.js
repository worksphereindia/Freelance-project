import { LabColorManager } from "./LabColorManager.js";
export async function loadLabColorPlugin(engine) {
    engine.checkVersion("4.3.2");
    await engine.pluginManager.register(e => {
        e.pluginManager.addColorManager("lab", new LabColorManager());
    });
}
