(function(g){g.__tsParticlesInternals=g.__tsParticlesInternals||{};g.__tsParticlesInternals.bundles=g.__tsParticlesInternals.bundles||{};g.__tsParticlesInternals.effects=g.__tsParticlesInternals.effects||{};g.__tsParticlesInternals.engine=g.__tsParticlesInternals.engine||{};g.__tsParticlesInternals.interactions=g.__tsParticlesInternals.interactions||{};g.__tsParticlesInternals.palettes=g.__tsParticlesInternals.palettes||{};g.__tsParticlesInternals.paths=g.__tsParticlesInternals.paths||{};g.__tsParticlesInternals.plugins=g.__tsParticlesInternals.plugins||{};g.__tsParticlesInternals.plugins=g.__tsParticlesInternals.plugins||{};g.__tsParticlesInternals.plugins.emittersShapes=g.__tsParticlesInternals.plugins.emittersShapes||{};g.__tsParticlesInternals.presets=g.__tsParticlesInternals.presets||{};g.__tsParticlesInternals.shapes=g.__tsParticlesInternals.shapes||{};g.__tsParticlesInternals.updaters=g.__tsParticlesInternals.updaters||{};g.__tsParticlesInternals.utils=g.__tsParticlesInternals.utils||{};g.__tsParticlesInternals.canvas=g.__tsParticlesInternals.canvas||{};g.__tsParticlesInternals.canvas=g.__tsParticlesInternals.canvas||{};g.__tsParticlesInternals.canvas.utils=g.__tsParticlesInternals.canvas.utils||{};g.__tsParticlesInternals.path=g.__tsParticlesInternals.path||{};g.__tsParticlesInternals.path=g.__tsParticlesInternals.path||{};g.__tsParticlesInternals.path.utils=g.__tsParticlesInternals.path.utils||{};var __tsProxyFactory=typeof Proxy!=="undefined"?function(obj){return new Proxy(obj,{get:function(target,key){if(!(key in target)){target[key]={};}return target[key];}});}:function(obj){return obj;};g.__tsParticlesInternals.bundles=__tsProxyFactory(g.__tsParticlesInternals.bundles);g.__tsParticlesInternals.effects=__tsProxyFactory(g.__tsParticlesInternals.effects);g.__tsParticlesInternals.interactions=__tsProxyFactory(g.__tsParticlesInternals.interactions);g.__tsParticlesInternals.palettes=__tsProxyFactory(g.__tsParticlesInternals.palettes);g.__tsParticlesInternals.paths=__tsProxyFactory(g.__tsParticlesInternals.paths);g.__tsParticlesInternals.plugins=__tsProxyFactory(g.__tsParticlesInternals.plugins);g.__tsParticlesInternals.plugins.emittersShapes=__tsProxyFactory(g.__tsParticlesInternals.plugins.emittersShapes);g.__tsParticlesInternals.presets=__tsProxyFactory(g.__tsParticlesInternals.presets);g.__tsParticlesInternals.shapes=__tsProxyFactory(g.__tsParticlesInternals.shapes);g.__tsParticlesInternals.updaters=__tsProxyFactory(g.__tsParticlesInternals.updaters);g.__tsParticlesInternals.utils=__tsProxyFactory(g.__tsParticlesInternals.utils);g.__tsParticlesInternals.canvas=__tsProxyFactory(g.__tsParticlesInternals.canvas);g.__tsParticlesInternals.path=__tsProxyFactory(g.__tsParticlesInternals.path);g.tsparticlesInternalExports=g.tsparticlesInternalExports||{};})(typeof globalThis!=="undefined"?globalThis:typeof window!=="undefined"?window:this);
/* Emitters Shape v4.3.2 */
(function (global, factory) {
    typeof exports === 'object' && typeof module !== 'undefined' ? factory(exports, require('@tsparticles/plugin-emitters'), require('@tsparticles/engine')) :
    typeof define === 'function' && define.amd ? define(['exports', '@tsparticles/plugin-emitters', '@tsparticles/engine'], factory) :
    (global = typeof globalThis !== 'undefined' ? globalThis : global || self, factory((global.__tsParticlesInternals = global.__tsParticlesInternals || {}, global.__tsParticlesInternals.plugins = global.__tsParticlesInternals.plugins || {}, global.__tsParticlesInternals.plugins.emittersShapes = global.__tsParticlesInternals.plugins.emittersShapes || {}, global.__tsParticlesInternals.plugins.emittersShapes.polygon = global.__tsParticlesInternals.plugins.emittersShapes.polygon || {}), global.__tsParticlesInternals.plugins.emitters, global.__tsParticlesInternals.engine));
})(this, (function (exports, pluginEmitters, engine) { 'use strict';

    const defaultRotation = 0, maxAttempts = 100;
    function generateRandomPolygon(position, sides, radius, rotationAngle = defaultRotation) {
        const polygon = [], angle = engine.doublePI / sides;
        for (let i = 0; i < sides; i++) {
            const currentAngle = angle * i + rotationAngle;
            polygon.push({
                x: position.x + radius * Math.cos(currentAngle),
                y: position.y + radius * Math.sin(currentAngle),
            });
        }
        return polygon;
    }
    function generateRandomPointWithinPolygon(polygon) {
        const firstIndex = 0, firstPoint = polygon[firstIndex];
        if (!firstPoint) {
            return null;
        }
        const min = { ...firstPoint }, max = { ...firstPoint };
        for (const point of polygon) {
            if (point.x < min.x) {
                min.x = point.x;
            }
            if (point.x > max.x) {
                max.x = point.x;
            }
            if (point.y < min.y) {
                min.y = point.y;
            }
            if (point.y > max.y) {
                max.y = point.y;
            }
        }
        let randomPoint = null;
        for (let attempts = 0; attempts < maxAttempts; attempts++) {
            const tmpPoint = {
                x: min.x + engine.getRandom() * (max.x - min.x),
                y: min.y + engine.getRandom() * (max.y - min.y),
            };
            if (isPointInPolygon(tmpPoint, polygon)) {
                randomPoint = tmpPoint;
                break;
            }
        }
        return randomPoint;
    }
    function generateRandomPointOnPolygonPerimeter(polygon) {
        const sideIndex = Math.floor(engine.getRandom() * polygon.length), startPoint = polygon[sideIndex];
        if (!startPoint) {
            return;
        }
        const offset = 1, endPoint = polygon[(sideIndex + offset) % polygon.length];
        if (!endPoint) {
            return;
        }
        const t = engine.getRandom();
        return { x: startPoint.x + (endPoint.x - startPoint.x) * t, y: startPoint.y + (endPoint.y - startPoint.y) * t };
    }
    function isPointInPolygon(point, polygon) {
        let inside = false;
        const offset = 1;
        for (let i = 0, j = polygon.length - offset; i < polygon.length; j = i++) {
            const pi = polygon[i], pj = polygon[j];
            if (!pi || !pj) {
                continue;
            }
            const intersect = pi.y > point.y !== pj.y > point.y && point.x < ((pj.x - pi.x) * (point.y - pi.y)) / (pj.y - pi.y) + pi.x;
            if (intersect) {
                inside = !inside;
            }
        }
        return inside;
    }

    class EmittersPolygonShape extends pluginEmitters.EmitterShapeBase {
        angle;
        polygon;
        sides;
        constructor(position, size, fill, options) {
            super(position, size, fill, options);
            this.sides = options.sides;
            this.angle = engine.degToRad(options.angle);
            this.polygon = generateRandomPolygon(position, this.sides, size.width * engine.half, this.angle);
        }
        async init() {
        }
        randomPosition() {
            const fill = this.fill, polygon = this.polygon, res = fill ? generateRandomPointWithinPolygon(polygon) : generateRandomPointOnPolygonPerimeter(polygon);
            return res ? { position: res } : null;
        }
        resize(position, size) {
            super.resize(position, size);
            this.polygon = generateRandomPolygon(position, this.sides, size.width * engine.half, this.angle);
        }
    }

    class EmittersPolygonShapeOptions {
        angle = 0;
        sides = 5;
        load(data) {
            if (engine.isNull(data)) {
                return;
            }
            if (data.angle !== undefined) {
                this.angle = data.angle;
            }
            if (data.sides !== undefined) {
                this.sides = data.sides;
            }
        }
    }

    class EmittersPolygonShapeGenerator {
        generate(_container, position, size, fill, options) {
            const shapeOptions = new EmittersPolygonShapeOptions();
            shapeOptions.load(options);
            return new EmittersPolygonShape(position, size, fill, shapeOptions);
        }
    }

    async function loadEmittersShapePolygon(engine) {
        engine.checkVersion("4.3.2");
        await engine.pluginManager.register((e) => {
            pluginEmitters.ensureEmittersPluginLoaded(e);
            e.pluginManager.addEmitterShapeGenerator?.("polygon", new EmittersPolygonShapeGenerator());
        });
    }

    const globalObject = globalThis;
    globalObject.__tsParticlesInternals = globalObject.__tsParticlesInternals ?? {};
    globalObject.loadEmittersShapePolygon = loadEmittersShapePolygon;

    exports.loadEmittersShapePolygon = loadEmittersShapePolygon;

}));
Object.assign(globalThis.window || globalThis, { loadEmittersShapePolygon: (globalThis.__tsParticlesInternals.plugins.emittersShapes.polygon || {}).loadEmittersShapePolygon });
delete (globalThis.window || globalThis).tsparticlesInternalExports;
