import { DrawLayer, getLogger, getStyleFromRgb, inverseFactorNumerator, rangeColorToRgb, safeDocument, } from "@tsparticles/engine";
const minimumLength = 0;
export class TrailPluginInstance {
    layer = DrawLayer.PluginContent;
    #container;
    #pluginManager;
    #trailFill;
    constructor(pluginManager, container) {
        this.#container = container;
        this.#pluginManager = pluginManager;
    }
    canvasClear() {
        const container = this.#container, trail = container.actualOptions.trail, trailFill = this.#trailFill;
        if (!trail?.enable || !trailFill || trail.length <= minimumLength) {
            return false;
        }
        let handled = false;
        const canvas = container.canvas;
        if (trailFill.color) {
            canvas.render.paintBase(getStyleFromRgb(trailFill.color, container.hdr, trailFill.opacity));
            handled = true;
        }
        else if (trailFill.image) {
            canvas.render.paintImage(trailFill.image, trailFill.opacity);
            handled = true;
        }
        return handled;
    }
    async init() {
        try {
            await this.#initTrail();
        }
        catch (e) {
            getLogger().error(e);
        }
    }
    async #initTrail() {
        const options = this.#container.actualOptions, trail = options.trail;
        if (!trail?.enable) {
            return;
        }
        const trailFill = trail.fill, opacity = inverseFactorNumerator / trail.length;
        if (trailFill.color) {
            const fillColor = rangeColorToRgb(this.#pluginManager, trailFill.color);
            if (!fillColor) {
                return;
            }
            this.#trailFill = {
                color: {
                    ...fillColor,
                },
                opacity,
            };
        }
        else {
            await new Promise((resolve, reject) => {
                if (!trailFill.image) {
                    return;
                }
                const img = safeDocument().createElement("img");
                img.addEventListener("load", () => {
                    this.#trailFill = {
                        image: img,
                        opacity,
                    };
                    resolve();
                });
                img.addEventListener("error", evt => {
                    getLogger().error(evt);
                    reject(new Error("Error loading image"));
                });
                img.src = trailFill.image;
            });
        }
    }
}
