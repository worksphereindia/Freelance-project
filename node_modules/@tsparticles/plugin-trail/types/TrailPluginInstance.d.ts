import { DrawLayer, type IContainerPlugin, type PluginManager } from "@tsparticles/engine";
import type { TrailContainer } from "./types.js";
export declare class TrailPluginInstance implements IContainerPlugin {
    #private;
    readonly layer: DrawLayer;
    constructor(pluginManager: PluginManager, container: TrailContainer);
    canvasClear(): boolean;
    init(): Promise<void>;
}
